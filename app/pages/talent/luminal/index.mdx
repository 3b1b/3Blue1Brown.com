---
name: Luminal AI
tagline: AI Inference Compilers
location: San Francisco
apply: https://www.luminal.com/careers
about: Luminal makes an inference compiler targeting datacenter accelerator directly from PyTorch models. By using large-scale kernel search, our compiler discovers deep optimizations automatically across heterogeneous hardware architectures. We run a first-party inference cloud on ASIC hardware and license our stack to neoclouds and foundation model labs.
---

import { ArrowUpRightIcon, BookIcon } from "@phosphor-icons/react";
import Button from "~/components/Button";
import Figure from "~/components/Figure";
import banner from "./banner.jpg";
import hardwareArbitrage from "./hardware-arbitrage.png";
import megakernels from "./megakernels.jpg";
import office from "./office.jpg";
import qwen3Throughput from "./qwen3-throughput.png";
import wordmark from "./wordmark.svg";

<section className="dark width-lg">

<img
  src={banner}
  alt=""
  className="absolute inset-0 -z-10 size-full object-cover object-right playing-fade"
/>

<img src={wordmark} alt="" className="max-h-20 self-center" />

<Button to={frontmatter.apply} color="critical" className="w-100 self-center">
  Apply
  <ArrowUpRightIcon />
</Button>

</section>

<section>

## About

Luminal builds an AI inference compiler that optimizes models for GPUs and ASICs, delivering the fastest, highest-throughput inference by treating compilation as a search problem.

<Figure image={office} />

</section>

<section>

## Featured work

### Compiling models to megakernels

Standard GPU inference wastes time on kernel launch overhead, uneven work distribution, and memory access delays between operations. Luminal's compiler fuses entire neural network models into single GPU kernels — megakernels — using a dynamic global instruction queue that schedules work across streaming multiprocessors at runtime, eliminating the gaps that conventional approaches leave behind.

<Figure image={qwen3Throughput} />

<Button
  to="https://blog.luminal.com/p/compiling-models-to-megakernels"
  color="light"
  className="self-center"
>
  <BookIcon />
  Read the post
</Button>

### Hardware arbitrage with Luminal and Positron AI

Different inference workloads have fundamentally different hardware needs: prefill is compute-bound while decode is bandwidth-bound. Luminal's compiler automatically routes each phase to the hardware it's best suited for — GPUs for prefill, Positron Atlas systolic arrays for decode — transferring KV cache between systems to squeeze out the maximum performance from heterogeneous infrastructure.

<Figure image={hardwareArbitrage} />

<Button
  to="https://blog.luminal.com/p/hardware-arbitrage-with-luminal-and"
  color="light"
  className="self-center"
>
  <BookIcon />
  Read the post
</Button>

</section>
